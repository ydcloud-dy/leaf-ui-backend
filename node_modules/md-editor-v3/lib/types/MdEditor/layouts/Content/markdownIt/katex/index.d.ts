/**
 * 源码来自https://github.com/waylonflinn/markdown-it-katex
 *
 * 该代码只是正对md-editor-v3系列功能做了适配
 */
import markdownit from 'markdown-it';
import { ShallowRef } from 'vue';
interface KatexOptions {
    katexRef: ShallowRef;
    inlineDelimiters?: Array<{
        open: string;
        close: string;
    }>;
    blockDelimiters?: Array<{
        open: string;
        close: string;
    }>;
}
declare const KatexPlugin: (md: markdownit, { katexRef, inlineDelimiters, blockDelimiters }: KatexOptions) => void;
export default KatexPlugin;
